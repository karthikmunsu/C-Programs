#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
int a[15],b[5],res[30];
int n=0,i=0,j=0,count=0,flag=0,leng;
printf("Enter the number of elements for first array : ");
scanf("%d",&n);
printf("Enter %d elements ",n);
for(i=0;i<n;i++){
scanf("%d",&a[i]);
}
    printf("Enter %d elements ",n);

for(i=0;i<n;i++){
scanf("%d",&b[i]);
    }
    leng=a[].length+b[].length;
    printf("%d",leng);

getch();
}